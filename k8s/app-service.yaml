apiVersion: v1
kind: Service
metadata:
  name: tc-oficina-service
  namespace: oficina
  labels:
    app.kubernetes.io/name: tc-oficina
    app.kubernetes.io/part-of: oficina-fiap
spec:
  type: NodePort
  selector:
    app: tc-oficina
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
